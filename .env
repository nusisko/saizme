# 'gcs' for Google Cloud Storage or 'local' for local filesystem
STORAGE_PROVIDER=gcs

# Your unique Google Cloud Storage bucket name
GCS_BUCKET_NAME=saizme

GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/gcs-key.json